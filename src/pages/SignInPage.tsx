export default function SignInPage() {
  return (
    <main className="flex min-h-svh flex-1 flex-col items-center justify-center px-4 py-12">
      <div className="w-full max-w-sm rounded-lg border border-[var(--border)] bg-[var(--bg)] p-8 text-left shadow-[var(--shadow)]">
        <h1 className="text-2xl font-medium text-[var(--text-h)]">
          Sign in
        </h1>
        <p className="mt-2 text-[var(--text)]">
          Login form placeholder — replace with credentials and submit handling.
        </p>
      </div>
    </main>
  )
}
